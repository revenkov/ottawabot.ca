import $ from 'jquery/src/jquery';

/*
import { gsap } from "gsap";

import { ScrollTrigger } from "gsap/ScrollTrigger";
import { ScrollToPlugin } from "gsap/ScrollToPlugin";

gsap.registerPlugin(ScrollTrigger,ScrollToPlugin);


const panelsContainer = document.querySelector("#timeline");
const panels = gsap.utils.toArray("#timeline .timeline__item");

let st = ScrollTrigger.create({
    trigger: "#timeline",
    pin: true,
    start: "top center",
    end: () => '+=' + document.querySelector('.timeline__items').outerHeight,
});

gsap.to(panels, {
    x: () => -1 * (panelsContainer.scrollWidth - innerWidth),
    ease: "none",
    scrollTrigger: {
        trigger: "#timeline",
        pin: true, // Pins the container to the viewport
        start: "top center",
        scrub: 1, // Ties the animation to the scrollbar
        end: () => `+=${panelsContainer.scrollWidth - innerWidth}` // Sets the scrollable distance
    }
});
 */


export default () => {

}